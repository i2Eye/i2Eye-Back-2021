from elasticsearch import Elasticsearch
from elasticsearch_dsl import Search, Q

# def index(client, index, json_dict):
#     client.create(index, "_doc", json_dict, refresh=True)
#     print('test success')
